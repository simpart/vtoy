/**
 * @file auth.js
 * @brief authentication function
 */
let mf = require('mofron');
module.exports = {
    auth : (msg) => {
        try {
            // auth process here
        } catch (e) {
            console.error(e.stack);
            throw e;
        }
    }
}
/* end of ifle */
